{
  "name": "@leo/e2e",
  "private": true,
  "type": "module",
  "description": "End-to-end tests: loads the built Leo extension into Chromium and records/replays workflows against local fixture sites.",
  "scripts": {
    "test": "playwright test",
    "typecheck": "tsc --noEmit",
    "fixtures": "bun run fixtures/server.ts"
  },
  "devDependencies": {
    "@leo/shared": "workspace:*",
    "@playwright/test": "1.63.0",
    "@types/bun": "^1.3.0",
    "@types/node": "^20",
    "typescript": "^5.9.3"
  }
}
