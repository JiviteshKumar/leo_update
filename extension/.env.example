# Copy to extension/.env and rebuild. Everything here is baked into the
# bundle and readable by anyone who installs the extension — no secrets.

# URL of the Leo web app (fe/). Defaults to http://localhost:3010.
WXT_LEO_FE_URL=http://localhost:3010
